server:
  applicationConnectors:
    - type: http
      port: ${APPLICATION_PORT:-2210}
  adminConnectors:
    - type: http
      port: ${ADMIN_PORT:-2211}

serviceProviderURI: ${SERVICE_PROVIDER_URI:-"http://localhost:2210"}

redisURI: ${REDIS_URI:-"redis://192.168.204.170:6380"}

directoryURI: ${DIRECTORY_URI:-"http://localhost:3000"}

middlewareURI: ${MIDDLEWARE_URI:-"http://localhost:3001"}

governmentBrokerURI: "http://localhost:6610"

rpURI: "http://localhost:4410"

local: ${IS_LOCAL:-true}

softwareID: "Trust Framework Service Provider"

scheme: ${SCHEME:-"1"}

orgID: ${ORG_ID:-"RP-1"}
